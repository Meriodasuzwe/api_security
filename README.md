# 🛡️ API Security Demo Stand

Учебный проект для демонстрации уязвимостей REST API и методов их защиты.
Проект реализует симуляцию атак (IDOR, SQL Injection, Brute-force) и позволяет в реальном времени включать/выключать механизмы защиты через админ-панель.

## 🏗️ Архитектура

* **Backend:** FastAPI (Python) — реализация API, JWT Auth, RBAC.
* **Database:** SQLite + SQLAlchemy (ORM).
* **Admin UI:** Streamlit — панель управления защитой и мониторинг логов.
* **Security:** Argon2 (hashing), JWT (Access/Refresh), Rate Limiting.

## 🚀 Установка и Запуск

1. **Клонируйте репозиторий:**
   ```bash
   git clone <ваша-ссылка-на-repo>
   cd courseproject